/** Tailwind is available for future feature modules; the admin shell uses its
 * own brand token stylesheet to keep the TUT visual system explicit. */
export default { content: ['./index.html', './src/**/*.{ts,tsx}'], theme: { extend: {} }, plugins: [] }
